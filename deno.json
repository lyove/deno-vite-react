{
  "tasks": {
    "dev": "deno run -A --node-modules-dir npm:vite",
    "build": "deno run -A --node-modules-dir npm:vite build",
    "preview": "deno run -A --node-modules-dir npm:vite preview",
    "serve": "deno run --allow-env --allow-net api/main.ts",
    "serve:example": "deno run --allow-net --allow-read https://deno.land/std@0.186.0/http/file_server.ts dist/"
  }
}
